Raspberry Pi software lives here.

- `pipeline/` runs the end-to-end translation loop.
- `gui/` contains the display and UI-side scripts.
- `passback/` and `scripts/` contain UART playback and utility tools.
