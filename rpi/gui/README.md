Raspberry Pi display/UI components.

- `display.py` is the primary display-side script.
- Setup and launch helpers are provided via shell scripts in this folder.
- Keep environment-specific dependencies local to this directory.
